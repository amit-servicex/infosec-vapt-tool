# name: web_default
# modules:
#   - id: zap_baseline
#     uses: web.zap.baseline
#     with: {}
#   - id: nuclei_basic
#     uses: web.nuclei.basic
#     with: {}


name: web_default
modules:
  - web.zap.baseline
  - web.nuclei.basic
  - report.html
extra:
  web.nuclei.basic:
    args: >
      -tags misconfig,exposure,cve
      -severity low,medium,high,critical
      -rate-limit 200 -c 100 -timeout 10000